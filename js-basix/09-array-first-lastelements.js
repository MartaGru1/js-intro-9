
const array = [ 'Mouse', 'Laptop', 'Computer', 'Phone', 'Camera' ];

//seze-lenght or how amny elements we have in this array ->5
console.log(array.length); //5

// How can I get the first element;

array[0]; // Mouse  -> always return(giving) first element

// How can I gt the last element
array[4]; // Camera    // array.lenght-1
array[array.length - 1]; // always giving the last element